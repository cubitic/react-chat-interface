import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';  // This line imports the CSS file with Tailwind directives
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);